const React = require('react');

function Default(html) {
  return (
    <html>
      <head>
        <title>React App</title>
      </head>
      <body>
        Hello World
      </body>
    </html>
  );
}

module.exports = Default;